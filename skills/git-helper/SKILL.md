---
name: git-helper
description: Everyday git helpers - branch hygiene, commit messages, safe undo.
---

# git-helper

Example skill shipped with the agent-workspace MVP.

## When to use

- Writing commit messages or preparing an interactive rebase
- Undoing, rewording or splitting commits safely
- Explaining what a git command will do before running it

## Notes

- Placeholder content: replace with your real skill.
- Enabled for every environment via `configs/common.yaml`.
## Update note (simulated edit on personal machine)

Prefer `git switch -c` over `git checkout -b` in examples.
