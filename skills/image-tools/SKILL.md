---
name: image-tools
description: Personal-only image processing and generation helpers.
---

# image-tools

Example skill for the `personal` environment.

## When to use

- Batch resizing / format conversion snippets
- Wiring up local image models

## Notes

- Enabled via `configs/personal.yaml`.
- Excluded on intranet machines (`exclude_skills` in `configs/intranet.yaml`).