---
name: jira-helper
description: Company Jira workflow helpers (tickets, sprints, queries).
---

# jira-helper

Example skill for the `company` environment.

## When to use

- Writing ticket updates and sprint summaries
- Building JQL queries for team boards

## Notes

- Enabled via `configs/company.yaml`.
- Requires the `COMPANY_TOKEN` secret (see `.env.example`).